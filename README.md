## VIT Vellore University Semester Data
### Open access for anyone to fetch data from `/calendars/[YYYY]/[Sem].json`
Auto-runs every 12 hours with latest semester data stored in `curr_sem.txt` to update the relevant Json
<br>
<br>

## Verify data:
Open `index.html` in any browser, and import json file to visialize it and manually verify data integrity.
